box: debian
build:
  steps:
    - arjen/hugo-build:
        theme: herring-cove
        flags: --buildDrafts=true
    - script:
        name: ls stuff
        code: |
          ls -haltr /pipeline/source/public