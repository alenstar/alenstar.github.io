box: debian
build:
  steps:
    - install-packages:
        packages: git  curl libcurl3
    - arjen/hugo-build:
        version: "1.16.3"
        theme: herring-cove
        flags: --buildDrafts=true